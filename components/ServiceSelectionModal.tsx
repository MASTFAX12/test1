// This component has been deprecated and its functionality removed as per user request.
// The new workflow involves a simplified payment process handled by the secretary.
// Keeping the file to avoid breaking import statements, but it renders nothing.

import React from 'react';

const ServiceSelectionModal: React.FC = () => {
    return null;
};

export default ServiceSelectionModal;
